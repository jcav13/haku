<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>about us - haku</title>
    <style>
        /* General styling */
        body {
            background-color: black;
            color: white;
            font-family: 'Helvetica Neue', sans-serif;
            text-align: center;
            margin: 0;
            padding: 0;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            position: relative;
            overflow-x: hidden;
            animation: fadeIn 1.5s ease-in-out;
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        /* Header */
        h1 {
            font-size: 50px;
            font-weight: 300;
            letter-spacing: 6px;
            margin-bottom: 20px;
            text-transform: lowercase;
        }

        /* About Container */
        .about-container {
            max-width: 800px;
            text-align: left;
            padding: 20px;
        }

        p {
            font-size: 18px;
            font-weight: 200;
            line-height: 1.6;
            text-transform: lowercase;
        }

        /* Buttons */
        .button-container {
            display: flex;
            gap: 20px;
            margin-top: 30px;
        }

        .button {
            display: inline-block;
            padding: 10px 30px;
            font-size: 16px;
            font-weight: 300;
            letter-spacing: 2px;
            color: white;
            background: transparent;
            border: 1px solid white;
            border-radius: 50px;
            text-decoration: none;
            transition: all 0.3s ease-in-out;
            text-transform: lowercase;
        }

        .button:hover {
            background: white;
            color: black;
            transform: scale(1.05);
        }
    </style>
</head>
<body>
    <h1>about us</h1>

    <div class="about-container">
        <p>haku is a high-end mobile massage therapy service, bringing relaxation and precision care to melbourne. inspired by traditional japanese practices, we deliver a seamless blend of luxury and authenticity.</p>

        <p>our philosophy is rooted in exceptional attention to detail, personalized care, and a commitment to excellence. every treatment is tailored to the individual, ensuring an experience that is both restorative and refined.</p>
    </div>

    <div class="button-container">
        <a href="index.html" class="button">home</a>
        <a href="services.html" class="button">services</a>
    </div>
</body>
</html>
